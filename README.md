# React SDD starter

A minimal React and TypeScript project. Product requirements and SDD artifacts will be added only after the requirement is defined.

## Run locally

```bash
npm install
npm run dev
```
